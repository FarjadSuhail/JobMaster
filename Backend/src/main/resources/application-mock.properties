# Mock AI mode: no real provider is configured, MockAiConfig supplies a canned
# ChatModel instead. Lets you exercise the whole API without spending credits:
#   ./gradlew bootRun --args='--spring.profiles.active=mock'
spring.ai.model.chat=none
